---
import Layout from '@/layouts/Layout.astro';
import BlogCard from '@/components/cards/BlogCard.astro';

const blogModules = import.meta.glob('../data/content-index/blog-*.json', { eager: true });
const blogPosts = Object.values(blogModules);
---
<Layout title="Blog">
  <div class="container mx-auto px-4 py-10 sm:py-16">
    <h1 class="text-3xl font-bold mb-8">Blog</h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {blogPosts.map(post => (
        <BlogCard post={{ data: post, slug: post.slug }} />
      ))}
    </div>
  </div>
</Layout>
